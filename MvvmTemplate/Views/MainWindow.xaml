<Window x:Class="MvvmTemplate.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MvvmTemplate.Views"
        xmlns:vm="clr-namespace:MvvmTemplate.ViewModels"
        xmlns:v="clr-namespace:MvvmTemplate.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="{Binding LoginUser.Name}" Height="450" Width="800">

    <Grid>
        <TabControl x:Name="MainTabControl" SelectedIndex="1" Margin="20">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon
                            Kind="Cog"
                            Width="18"
                            Height="18"
                            Margin="0,0,6,0" />
                        <TextBlock
                            Text="Settings"
                            VerticalAlignment="Center" />
                    </StackPanel>
                </TabItem.Header>
                <TabItem.Content>
                    <v:SettingsTabView x:Name="SettingsTab" />
                </TabItem.Content>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon
                            Kind="BarChart"
                            Width="18"
                            Height="18"
                            Margin="0,0,6,0" />
                        <TextBlock
                            Text="Statistics"
                            VerticalAlignment="Center" />
                    </StackPanel>
                </TabItem.Header>
                <TabItem.Content>
                    <v:StatisticsTabView x:Name="StatisticsTab" />
                </TabItem.Content>
            </TabItem>
            <TabItem HorizontalAlignment="Right">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon
                            Kind="Information"
                            Width="18"
                            Height="18"
                            Margin="0,0,6,0" />
                        <TextBlock
                            Text="About"
                            VerticalAlignment="Center" />
                    </StackPanel>
                </TabItem.Header>
                <TabItem.Content>
                    <v:AboutTabView x:Name="AboutTab" />
                </TabItem.Content>
            </TabItem>
        </TabControl>
    </Grid>

</Window>
